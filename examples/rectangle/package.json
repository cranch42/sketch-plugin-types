{
  "name": "sketch-example-rectangle",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/bundle.js",
    "install-plugin": "npm run build && rm -rf \"$HOME/Library/Application Support/com.bohemiancoding.sketch3/Plugins/Rectangle.sketchplugin\" && cp -R build/Rectangle.sketchplugin \"$HOME/Library/Application Support/com.bohemiancoding.sketch3/Plugins/\""
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "sketch-plugin-types": "file:../.."
  }
}
